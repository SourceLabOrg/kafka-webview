server:
  session:
    persistent: true

security:
  require-ssl: false

spring:
  # H2 Datbase
  h2:
    console:
      ## Disable for release
      enabled: true
      path: /h2

  jpa:
    show-sql: true